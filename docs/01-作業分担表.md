# Web UIコンポーネント辞典 — データ作成ルール

## このプロジェクトについて
HTML/CSS/JavaScriptの要素・プロパティ・APIを網羅したリファレンスアプリ。
データは `data/` フォルダ内のJSONファイルで管理し、`data/_index.json` で有効/無効を切り替える。

## JSONファイル作成時のルール

### フォーマット
data/ フォルダ内の既存JSONファイル（例: data/01-text.json）を必ず参照し、同じ構造で作成すること。

基本構造:
- cat: カテゴリ名
- catIcon: 絵文字1つ
- catDesc: カテゴリ説明（1-2文）
- items: 配列。各項目は以下のキーを持つ
  - name: 要素名（HTMLタグは &lt;div&gt; のようにエスケープ）
  - label: "HTML" / "CSS" / "JS" のいずれか
  - labelClass: "label-html" / "label-css" / "label-js" のいずれか
  - desc: 説明文（2-3文）
  - demo: インラインstyleで自己完結するHTMLデモ。外部CSS/JS依存なし。font-size 0.85rem基準
  - code: コピペで使えるコードスニペット。コメント付き推奨
  - props: 属性/プロパティの配列（4-8個）。各要素は { "name": "コード表記", "desc": "日本語説明" }
  - tags: 分類タグの配列（2-4個）
  - browser: ブラウザ対応状況（「全ブラウザ対応」or 具体バージョン）
  - mdn: MDNのURL（日本語版優先）

### 品質基準
- 1カテゴリ 8-18項目
- demoは必ずインラインstyleで完結させる（外部リソース読込禁止）
- codeはベストプラクティスに従う
- 既存カテゴリとの内容重複は避ける

### 作成後の手順
1. data/ フォルダにJSONファイルを配置
2. data/_index.json の categories 配列に追加し enabled: true にする

## 既存ファイル一覧（完成済み）
| ファイル | カテゴリ名 |
|---------|----------|
| 01-text.json | テキスト・見出し |
| 02-form.json | フォーム・入力 |
| 03-button.json | ボタン・リンク |
| 04-layout.json | レイアウト・構造 |
| 05-list-table.json | リスト・テーブル |
| 06-media.json | メディア・埋め込み |
| 07-interactive.json | インタラクティブ要素 |
| 08-css-effect.json | CSSデコレーション・効果 |
| 09-responsive.json | レスポンシブ・メディアクエリ |
| 10-a11y.json | アクセシビリティ |
| 11-js-api.json | JavaScript Web API |
| 12-metadata.json | メタデータ・ドキュメント |
| 13-css-typography.json | CSSタイポグラフィ |
| 14-css-boxmodel.json | CSSボックスモデル |
| 15-css-color-bg.json | CSSカラー・背景 |
| 16-css-selector.json | CSSセレクタ・疑似クラス |
| 17-ui-pattern.json | UIパターン |
| 18-modern-css.json | モダンCSS新機能 |
| 19-js-async.json | JS非同期・通信 |
| 20-js-storage.json | JSストレージ・データ |
| 21-js-device.json | デバイス・ハードウェアAPI |
| 24-css-print.json | CSS印刷・特殊メディア |

## 未着手カテゴリ（作成依頼があったら作る）
| ファイル | カテゴリ名 | 項目数目安 | 想定内容 |
|---------|----------|-----------|---------|
| 22-performance.json | パフォーマンス最適化 | 12-15 | Performance Observer, Core Web Vitals, Resource Hints, requestIdleCallback, content-visibility, will-change, contain, 画像最適化, Code Splitting, debounce/throttle, PerformanceMark |
| 23-svg-detail.json | SVG詳細 | 12-15 | 基本図形, path, viewBox, text/tspan, defs/use/symbol, SVGグラデーション, filter, mask/clipPath, stroke-dasharray, animate, foreignObject, スプライト |
| 25-css-functions.json | CSS関数・計算 | 10-12 | calc(), env(), round()/mod()/rem(), abs()/sign(), color-mix(), light-dark(), color(), image-set(), counter()/counters(), fit-content()/minmax() |
| 26-web-components.json | Web Components | 10-12 | Custom Elements, Shadow DOM, template, slot, ::part(), :host, Declarative Shadow DOM, adoptedStyleSheets, Custom Events, Lifecycle callbacks |
| 27-js-modern-syntax.json | モダンJS構文 | 12-15 | ?., ??, &&=/||=/??=, Array.at(), Object.hasOwn(), Promise.withResolvers(), Object.groupBy(), Array.fromAsync(), using, Iterator helpers, Set methods, import.meta, Top-level await |
| 28-css-2026.json | CSS 2026最新機能 | 10-12 | base-select, ::picker, ::scroll-button/marker, sibling-index()/count(), @starting-style, interpolate-size, CSS if(), scroll-state queries, @function, invoker commands, popover="hint" |
| 29-forms-advanced.json | フォーム応用 | 8-10 | Constraint Validation API, setCustomValidity, フローティングラベル, inputmode, enterkeyhint, autocomplete詳細, 非同期送信, マルチステップ, リアルタイムバリデーション |
| 30-js-dom-advanced.json | DOM操作応用 | 10-12 | TreeWalker, Range/Selection, DocumentFragment, CustomEvent, EventTarget, DOMParser, template.content.cloneNode, getComputedStyle, dataset, closest()/matches() |

## 参照すべきファイル
新しいJSONを作る時は、必ず data/01-text.json を実際に読んでフォーマットを確認すること。
